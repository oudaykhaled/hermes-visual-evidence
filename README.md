# Hermes Visual Evidence

Direct-link delivery for Hermes fleet QA evidence (the Slack bot lacks `files:write`).
Each folder = one deliverable run. Images render inline via raw links; recordings open on the blob page.
